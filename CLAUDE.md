# Working rules for this repo

- Every bug fix or behaviour change gets an entry in `docs/FIXLOG.md` in the
  same change: what was seen, the real cause with file and function, what
  changed, how it was checked, and any trap that made it hard to find.
  A fix without an entry is not finished.
- Read `docs/FIXLOG.md` before debugging anything that looks familiar.
